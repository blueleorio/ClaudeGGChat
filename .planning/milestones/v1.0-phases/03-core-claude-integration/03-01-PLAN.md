---
phase: 03-core-claude-integration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/__tests__/claude.test.ts
  - src/__tests__/cards.test.ts
  - src/__tests__/chatEvent.test.ts
autonomous: true
requirements: [CLDE-01, CLDE-03, CLDE-04, CLDE-05, RESP-01, RESP-02, RESP-03]

must_haves:
  truths:
    - "npm install @anthropic-ai/sdk completes without error and the package appears in package.json dependencies"
    - "npx jest runs and the three new test files are collected (tests fail red — that is correct at Wave 0)"
    - "claude.test.ts has failing stubs covering callClaude model, system prompt, rate limit, overload (529), and timeout cases"
    - "cards.test.ts has failing stubs covering buildReplyCard and buildErrorCard outputs"
    - "chatEvent.test.ts has failing stubs covering the Thinking→PATCH lifecycle and the plain-text fallback"
  artifacts:
    - path: "src/__tests__/claude.test.ts"
      provides: "Unit test stubs for anthropicClient (CLDE-01, CLDE-04, CLDE-05)"
      exports: []
    - path: "src/__tests__/cards.test.ts"
      provides: "Unit test stubs for buildReplyCard and buildErrorCard (RESP-01, RESP-02)"
      exports: []
    - path: "src/__tests__/chatEvent.test.ts"
      provides: "Unit test stubs for async lifecycle and fallback (CLDE-03, RESP-03)"
      exports: []
  key_links:
    - from: "src/__tests__/claude.test.ts"
      to: "src/claude/anthropicClient.ts"
      via: "jest.mock('@anthropic-ai/sdk')"
      pattern: "import.*anthropicClient"
    - from: "src/__tests__/chatEvent.test.ts"
      to: "src/chat/chatClient.ts"
      via: "jest.mock('../chat/chatClient')"
      pattern: "jest\\.mock.*chatClient"
---

<objective>
Install the Anthropic SDK and create the three Wave 0 test scaffold files that must fail RED before any implementation begins. This is the mandatory TDD gate: tests exist before the code they test.

Purpose: Establishes the verification contract for all Phase 3 requirements so every subsequent plan has an automated green/red signal.
Output: @anthropic-ai/sdk in package.json; three failing test files that become green as Plans 02 and 03 implement their targets.
</objective>

<execution_context>
@C:/Users/BeluKotu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BeluKotu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-claude-integration/03-VALIDATION.md
@.planning/phases/03-core-claude-integration/03-RESEARCH.md

<interfaces>
<!-- Existing contracts the test stubs must reference. Extracted from codebase. -->
<!-- Do NOT implement these yet — only import them in test files so they fail. -->

From src/chat/cards.ts:
```typescript
export function buildUsageHintCard(): object
// Wave 0: extend with buildReplyCard and buildErrorCard (not yet written)
```

From src/handlers/chatEvent.ts:
```typescript
export async function handleChatEvent(req: Request, res: Response): Promise<void>
// Async block: setImmediate void async pattern — Phase 3 expands this
```

From src/__tests__/webhook.test.ts (pattern to follow):
```typescript
jest.mock('google-auth-library');
// Uses jest.fn().mockResolvedValue / mockRejectedValue pattern
// Imports app from '../index' via supertest
```

From src/__tests__/cards.test.ts (pattern to follow):
```typescript
import { buildUsageHintCard } from '../chat/cards';
describe('buildUsageHintCard', () => { it('...', () => { ... }) })
// Extend this file to add buildReplyCard and buildErrorCard stubs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install @anthropic-ai/sdk and create claude.test.ts scaffold</name>
  <files>package.json, package-lock.json, src/__tests__/claude.test.ts</files>
  <behavior>
    - Test 1: callClaude() calls anthropic.messages.create with model 'claude-sonnet-4-6' (CLDE-01)
    - Test 2: callClaude() passes SEV_SYSTEM_PROMPT as the system field (CLDE-01)
    - Test 3: callClaude() passes { timeout: 25_000 } as the per-request option (CLDE-05)
    - Test 4: callClaude() returns the text content from message.content[0].text (CLDE-01)
    - Test 5: RateLimitError thrown by SDK → callClaude() rejects with RateLimitError (CLDE-04)
    - Test 6: InternalServerError with status 529 thrown by SDK → callClaude() rejects with that error (CLDE-04)
    - Test 7: APIConnectionTimeoutError thrown by SDK → callClaude() rejects with APIConnectionTimeoutError (CLDE-05)
  </behavior>
  <action>
    Run `npm install @anthropic-ai/sdk` in the project root. Verify it appears in package.json dependencies.

    Create `src/__tests__/claude.test.ts`. The file must:
    - Import `callClaude` from `'../claude/anthropicClient'` (module does not exist yet — import will fail at test run, which is correct Wave 0 behavior)
    - Mock `@anthropic-ai/sdk` using `jest.mock('@anthropic-ai/sdk')` so tests are isolated from the real API
    - Write the 7 behavior stubs listed above as `it` blocks inside `describe('callClaude', ...)`
    - For SDK error class mocking, use `jest.fn()` on `anthropic.messages.create` to throw `new Anthropic.RateLimitError(429, {...}, 'Rate limited', new Headers())` etc. — or mock the error classes directly
    - Tests MUST FAIL with "Cannot find module '../claude/anthropicClient'" at Wave 0. That is correct.

    Do NOT create `src/claude/anthropicClient.ts` in this task — that is Plan 02's job.
  </action>
  <verify>
    <automated>cd D:/SEV/VSCODE/ClaudeGGChat && node -e "require('./node_modules/@anthropic-ai/sdk')" && echo "SDK installed" && npx jest --testPathPattern=claude 2>&1 | grep -E "(FAIL|Cannot find module|Tests:)" | head -5</automated>
  </verify>
  <done>@anthropic-ai/sdk present in node_modules; npx jest --testPathPattern=claude exits with failure citing "Cannot find module '../claude/anthropicClient'" (RED — correct)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend cards.test.ts and create chatEvent.test.ts scaffold</name>
  <files>src/__tests__/cards.test.ts, src/__tests__/chatEvent.test.ts</files>
  <behavior>
    cards.test.ts additions:
    - Test 1: buildReplyCard(text) returns object with cardsV2 array (RESP-01)
    - Test 2: buildReplyCard(text) card header title is 'Claude' (RESP-01)
    - Test 3: buildReplyCard(text) card has at least one section widget containing the reply text (RESP-01)
    - Test 4: buildErrorCard('some error') returns object with cardsV2 array (RESP-02)
    - Test 5: buildErrorCard('some error') card header title is 'Claude' (RESP-02)
    - Test 6: buildErrorCard('some error') card widget text contains the error message (RESP-02)

    chatEvent.test.ts (new file):
    - Test 1: handleChatEvent with non-empty prompt calls chatClient.spaces.messages.create with cardsV2 Thinking card (CLDE-03)
    - Test 2: handleChatEvent async block calls chatClient.spaces.messages.patch with Claude's reply after callClaude resolves (CLDE-03)
    - Test 3: handleChatEvent async block calls chatClient.spaces.messages.patch with error card when callClaude rejects (CLDE-04/05 via integration)
    - Test 4: If the first patch (cardsV2) throws, a second patch with updateMask 'text' is attempted (RESP-03 fallback)
  </behavior>
  <action>
    **Extend `src/__tests__/cards.test.ts`:**
    Add a new `describe('buildReplyCard', ...)` block and `describe('buildErrorCard', ...)` block after the existing `buildUsageHintCard` tests. Import `buildReplyCard` and `buildErrorCard` from `'../chat/cards'` at the top of the file (these exports do not exist yet — that is correct Wave 0).

    **Create `src/__tests__/chatEvent.test.ts`:**
    - Mock `'../chat/chatClient'` with `jest.mock('../chat/chatClient', () => ({ chatClient: { spaces: { messages: { create: jest.fn(), patch: jest.fn() } } } }))`
    - Mock `'../claude/anthropicClient'` with `jest.mock('../claude/anthropicClient', () => ({ callClaude: jest.fn() }))`
    - Import `handleChatEvent` from `'../handlers/chatEvent'`
    - Import `chatClient` from `'../chat/chatClient'` (mocked)
    - Import `callClaude` from `'../claude/anthropicClient'` (mocked)
    - Write tests using `supertest` is NOT needed here — call `handleChatEvent(req, res)` directly with mock req/res objects
    - For async behavior: after calling handleChatEvent, use `await new Promise(resolve => setImmediate(resolve))` to let the async setImmediate block run before asserting
    - Mock req structure: `{ body: { message: { slashCommand: {}, argumentText: ' hello', thread: { name: 'spaces/X/threads/t1' } }, space: { name: 'spaces/X' } } }`
    - Mock res: `{ status: jest.fn().mockReturnThis(), json: jest.fn().mockReturnThis() }`

    Tests MUST FAIL at Wave 0 because `'../claude/anthropicClient'` and `'../chat/chatClient'` modules do not exist yet.
  </action>
  <verify>
    <automated>cd D:/SEV/VSCODE/ClaudeGGChat && npx jest --testPathPattern="(cards|chatEvent)" 2>&1 | grep -E "(FAIL|PASS|Cannot find module|Tests:)" | head -10</automated>
  </verify>
  <done>npx jest --testPathPattern="(cards|chatEvent)" runs; cards.test.ts fails on missing buildReplyCard/buildErrorCard exports; chatEvent.test.ts fails on missing modules — all RED (correct Wave 0 state)</done>
</task>

</tasks>

<verification>
Full Wave 0 gate:
```
cd D:/SEV/VSCODE/ClaudeGGChat && npx jest 2>&1 | tail -20
```
Expected: existing health.test.ts and webhook.test.ts remain GREEN; claude.test.ts, cards.test.ts (new describe blocks), chatEvent.test.ts are RED with module-not-found or missing-export errors.
</verification>

<success_criteria>
- `@anthropic-ai/sdk` appears in package.json dependencies
- `src/__tests__/claude.test.ts` exists with 7 test stubs, fails RED citing missing anthropicClient module
- `src/__tests__/cards.test.ts` has buildReplyCard + buildErrorCard stubs appended, fails RED on missing exports
- `src/__tests__/chatEvent.test.ts` exists with 4 test stubs, fails RED citing missing chatClient/anthropicClient modules
- Previously passing tests (health, webhook) remain GREEN
- No watch-mode flags used in any verify command
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-claude-integration/03-01-SUMMARY.md`
</output>
