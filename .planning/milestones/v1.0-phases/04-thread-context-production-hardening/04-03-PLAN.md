---
phase: 04-thread-context-production-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified: []
autonomous: false
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CLDE-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "Railway deployment is live with Phase 4 code"
    - "Thread context works end-to-end: asking 'what did I just say?' returns an accurate answer referencing the prior message"
    - "Bot's own messages do not appear in its context (no self-referential replies)"
    - "Structured log lines appear in Railway logs with requestId, spaceId, command, latencyMs, status"
    - "Server starts correctly on Railway (no startup crash due to validateEnv)"
  artifacts:
    - path: "src/utils/validateEnv.ts"
      provides: "Production startup validation"
    - path: "src/handlers/chatEvent.ts"
      provides: "Thread context injection + structured logging in production"
  key_links:
    - from: "Railway deployment"
      to: "Google Chat"
      via: "live /claude invocation producing context-aware reply"
      pattern: "Railway log: thread context fetch + PATCH"
---

<objective>
Deploy Phase 4 code to Railway and perform live verification of thread context, logging, and startup validation.

Purpose: Confirm all Phase 4 user-visible behaviors work end-to-end in production. Unit tests proved correctness; this checkpoint proves integration.
Output: Live Railway deployment with context-aware Claude replies and structured logs confirmed by human.
</objective>

<execution_context>
@C:/Users/BeluKotu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/BeluKotu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-thread-context-production-hardening/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy Phase 4 to Railway</name>
  <files></files>
  <action>
    1. Confirm all tests pass locally: `npx jest` — must exit 0
    2. Confirm TypeScript compiles: `npx tsc --noEmit` — must exit 0
    3. Push to Railway via git push (Railway auto-deploys from the connected git branch):
       `git add -A && git commit -m "feat(04): thread context, startup validation, structured logging" && git push`
    4. Monitor Railway deployment logs at https://railway.app — confirm:
       - Build succeeds (no TypeScript compilation errors)
       - Server starts without "Missing required environment variables" error (all 3 env vars are set in Railway)
       - `GET /health` returns 200 (Railway health check passes)
    5. If startup validation triggers unexpectedly: check Railway environment variables panel — all three of ANTHROPIC_API_KEY, ALLOWED_SPACE_IDS, GOOGLE_SERVICE_ACCOUNT_KEY must be set.
  </action>
  <verify>
    <automated>npx jest && npx tsc --noEmit</automated>
  </verify>
  <done>Railway deployment is live, server started without errors, health check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification of thread context, logging, and startup validation</name>
  <action>Human verifies Phase 4 behaviors end-to-end in the authorized Google Chat test space and Railway logs.</action>
  <what-built>
    Thread context injection: Claude now receives the last 10 messages from the current thread before answering.
    Startup validation: Server exits loudly if required env vars are missing.
    Structured logging: Each invocation emits a JSON log line with requestId, spaceId, command, latencyMs, status.
  </what-built>
  <how-to-verify>
    **Test 1 — Thread context (CONT-01, CLDE-02):**
    1. In the authorized test space (spaces/AAAA8WYwwy4), send a normal message: "The incident started at 14:00 UTC"
    2. Wait ~2 seconds
    3. Send: `/claude what time did the incident start?`
    4. Expected: Claude's reply references "14:00 UTC" — it incorporated the thread context

    **Test 2 — "What did I just say?" verification (CONT-01, CLDE-02):**
    1. In the same thread, send: "My favorite color is blue"
    2. Send: `/claude what did I just say?`
    3. Expected: Claude's reply mentions "blue" or "favorite color"

    **Test 3 — Bot message exclusion (CONT-02):**
    1. Look at Claude's reply from Test 1 or 2
    2. Send: `/claude how many messages have you sent in this thread?`
    3. Expected: Claude should not be "aware" of its own prior messages as user messages — the bot's cards/replies are excluded from context. The reply will not list its own previous responses as prior human messages.
    (Note: This test is qualitative — bot responses are filtered before being sent to Claude.)

    **Test 4 — Structured logging (INFRA-04):**
    1. Open Railway logs after any `/claude` invocation
    2. Look for a JSON log line (not the startup "Server listening" line)
    3. Expected: A line like `{"requestId":"...","spaceId":"spaces/AAAA8WYwwy4","command":"what did I just say?","latencyMs":3421,"status":"ok"}`
    4. Verify: requestId is a UUID-like string, latencyMs is a number > 0, status is "ok"

    **Test 5 — Startup validation (INFRA-03):**
    Already verified implicitly — if the Railway deployment is live, validateEnv() found all required vars. No separate test needed (would require temporarily removing an env var from Railway, which is disruptive).
  </how-to-verify>
  <verify>
    <automated>MISSING — live Railway + Google Chat verification required</automated>
  </verify>
  <done>All 4 live tests pass: thread context reply references prior message, bot exclusion confirmed, JSON log line visible in Railway logs with required fields.</done>
  <resume-signal>Type "approved" if all verifications pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete when:
- All unit tests pass: `npx jest` exits 0
- Railway deployment live: `GET https://claudeggchat-production.up.railway.app/health` returns 200
- Thread context verified: Claude's replies in the test space reference prior thread messages
- Structured logs confirmed: JSON log lines visible in Railway logs after `/claude` invocations
- ROADMAP.md Phase 4 marked complete
</verification>

<success_criteria>
- Railway deployment live with Phase 4 code, server started without validation errors
- Claude's reply to "what did I just say?" accurately references the prior message (Phase 4 primary user value confirmed)
- JSON log lines visible in Railway logs with all required fields (requestId, spaceId, command, latencyMs, status)
- All 6 Phase 4 requirements (CONT-01, CONT-02, CONT-03, CLDE-02, INFRA-03, INFRA-04) verified
- v1.0 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-thread-context-production-hardening/04-03-SUMMARY.md`
</output>
